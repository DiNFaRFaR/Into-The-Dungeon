using UnityEngine;

public class Corridor : MonoBehaviour
{
    public DoorPoint startPoint; // Snap this to the Room's exit
    public DoorPoint exitPoint;  // The new "Open Door" for the next room
}